#macro(upperFirstChar $typeName)$typeName.substring(0,1).toUpperCase()$typeName.substring(1)#end
import com.stone.tools.template.TemplateClass;
/**
 * The template of $templateName;<br>
 *
 * Auto generated by tools, do not modified;
 *
 * @author crazyjohn
 *
 */
 @TemplateClass(templateFile = "xml/${templateName}.xml")
public class $templateName {
#foreach($field in $fields)
    /** $field.getComment()s */
	protected $field.getTypeName() $field.getName();
#end
	
#foreach($field in $fields)
	public $field.getTypeName() get#upperFirstChar($field.getName())() {
		return $field.getName();
	}
	
	public void set#upperFirstChar($field.getName())($field.getTypeName() $field.getName()) {
		this.$field.getName() = $field.getName();
	}
#end

}